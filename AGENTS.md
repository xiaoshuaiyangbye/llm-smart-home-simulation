# 智能家居仿真：Evidence-First Codex

本仓库的首要标准是论文级的可追溯性、可复现性与结论边界，而不是增加功能数量或让 Agent 显得更自主。

## 每次任务的起点

1. 先运行 `git status --short`；保留并避开所有既有未提交修改。
2. 先检索 `docs/improvement_backlog.md`，不要重做已经验证的工作。
3. 将请求归类为只读诊断、单一可验证修复、研究设计决策或普通产品改动。
4. 对论文 QA、复现实验、消融、RAG、个性化或自治控制问题，使用 `.agents/skills/smart-home-thesis-qa-loop/`。

## 证据优先原则

- 每轮只处理一个有直接证据支持的最高影响问题。没有已证实问题时，完成质量门禁后停止，不制造改动。
- 结论必须区分软件仿真、mock 语义、真实模型、本地服务与真实设备/现场验证；不得把前者表述为后者。
- 最小改动优先。不要为顺手重构、预测性修复或未验证优化扩大范围。
- 不提交、推送、部署、合并、删除数据或修改密钥/外部系统，除非用户明确授权。

## 验证与证据账本

- 验证应与改动风险相称：后端使用 `backend\.venv\Scripts\python.exe`；前端命令从 `frontend/` 运行。
- 论文 QA 或用户可见行为改动，除测试/构建外还应检查 `http://localhost/health`、`/api/agent/health`，并在适用时用 Edge 或应用内浏览器获取真实页面证据。
- 一次已验证修复必须在 `docs/improvement_backlog.md` 记录：问题、最小修复、验证证据、研究含义、研究边界与残余风险。
- 广泛中文文档改写后，运行 `scripts/evaluate_rag.py --min-recall 1.0`，并保留必要的英文命令、API、路径与检索关键词。

## 协作与治理

- 当前应用内多智能体是中心编排、可审计的流程；不得将其描述成去中心化的自主协商系统。
- 默认不要并行委派。仅在任务可独立拆分且能带来实质性证据价值时使用子代理；并行写入共享工作区通常不合适。
- 涉及实验结果、方法对比、论文表述或真实/模拟边界时，可显式委派 `.codex/agents/research-claim-critic.toml` 做一次只读审计。审计意见不替代测试、真实浏览器验证或人工研究设计决策。

## 沟通

- 默认中文汇报；保留必要的英文命令、API 名称、文件路径与检索关键词。
- 交付时先给结论，再给实际证据、边界与未完成风险。
