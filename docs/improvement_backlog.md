# 持续改进待办

## Research QA updates

| 2026-07-12 | Completed | Local embedding retrieval was recomputed from source on each process start, its three-case check did not bind itself to an evaluation-suite version, and its top results could repeat one document while excluding a distinct relevant source. Separately, real local semantic repeats did not persist the initialized simulator input fingerprint, while an intermittent empty response from Ollama's OpenAI-compatible endpoint could exhaust the retry budget. | Added a checksummed, content-fingerprinted persistent RAG index with safe rebuild on corrupt/configuration-incompatible cache; changed local embedding retrieval to an explicit vector-plus-lexical hybrid with source diversity; expanded the versioned suite to six retrieval cases and records its SHA-256. Added an executable field-evidence validator that requires isolated-device, rollback, emergency-stop, calibration, baseline, raw-log, and manifest evidence before a real-device claim can be accepted. Reproduction records now include an initial-state SHA-256; unqualified single-light switches are normalized to deterministic basic-device semantics; a local-only native Ollama `/api/chat` fallback is explicit in `llm_metrics.transport` when the OpenAI-compatible response is empty. E2E now forces mock semantics so parallel browser checks do not contend for Ollama, and the discovered satisfaction-button contrast defect was corrected. | Evidence: lexical and live `qwen3-embedding:0.6b` hybrid RAG both achieved Recall@3 6/6 with cache persistence enabled; corruption and restore paths have regression tests. The local `qwen3:8b` three-task/three-repeat artifact at `output/qa-real-ollama-stability-v2/20260712_195022` has fixed per-task initial-state fingerprints and an intact manifest; P001/P002 were stable across all three attempts, while P003 exposed an empty-response failure after three OpenAI-compatible attempts and therefore correctly withholds aggregate stability. Final verification: backend pytest 80/80, backend/test/script Ruff, `pip check`, Docker Compose config, frontend Vitest, 4/4 mock-isolated Playwright checks (including WCAG A/AA), and the bundle gate passed (2D initial JavaScript 183,254 bytes; deferred 3D 275,107 bytes gzip). Research boundary: six curated retrieval cases and three task diagnostics are not broad accuracy, latency, or load evidence; real-device conclusions still require completed field evidence and actual hardware. Remaining risk: P003's `turn_on` versus `set_target` label remains unadjudicated, and a complete benchmark needs independent label review plus wider task/category coverage. |

| 2026-07-12 | Completed | The RAG path only used deterministic lexical cosine scoring, so a local embedding model could be installed and verified while never participating in retrieval. Treating that path as vector RAG would overstate the retrieval method. | `RagDocumentStore` now supports `RAG_RETRIEVAL_MODE=local_embedding` using Ollama's local `/api/embed` endpoint and records the active engine/model in every retrieval result. The local profile selects `qwen3-embedding:0.6b`; invalid endpoint responses or unavailable Ollama fail closed to the established lexical scorer with an explicit fallback reason, preserving CI and offline behavior. | Evidence: live Ollama embedding probe returned two 1024-dimensional vectors; a live project query reported `engine=local_ollama_embedding`, model `qwen3-embedding:0.6b`, and three matches. Unit coverage verifies vector ranking and deterministic lexical fallback; focused Ruff and pytest passed. Research boundary: this establishes locally served embedding retrieval only, not broad retrieval quality, reranking quality, semantic answer quality, or real-home outcomes. Remaining risk: embedding index construction is in-memory and recomputed after source changes; a large corpus requires persistent vector storage and retrieval benchmarks. |

| 2026-07-12 | Completed | The local development environment was configured for an unspecified non-local real-LLM endpoint while Docker targeted local Ollama, and neither path supplied an immutable model revision. Ollama was installed with `qwen3:8b`, but its service was not running; after startup, the OpenAI-compatible streaming path intermittently ended without content. This meant a `real` UI label alone did not prove a reproducible local semantic run. | Added an ignored `backend/.env.local` override layer, loaded before the shared `.env` by the API, client, and connectivity script. The local profile targets `http://127.0.0.1:11434/v1`, binds the installed `qwen3:8b` digest, uses temperature 0/seed 42, raises the local timeout to 30 seconds, and disables the unstable stream path. Added a tracked example and README instructions; connection health now reports the model revision without exposing any credential. | Evidence: Ollama `/api/tags` was reachable after service startup; installed `qwen3:8b` and `qwen3-embedding:0.6b` were present, and the deployment model matched the local chat model. A real semantic probe returned `success=true`, `llm_mode=real`, the configured model revision, `stream=false`, and a model-generated semantic result; backend pytest 71/71, Ruff, and RAG Recall@3 3/3 = 1.0 passed. Research boundary: the RAG retriever is real local lexical cosine retrieval over versioned project files, not an embedding/vector-database benchmark; one successful local-model probe does not establish semantic accuracy, latency, concurrency, safety, or generalization. Remaining risk: the local model took roughly 18 seconds and required one retry in this probe, so real-model performance claims require a separately versioned multi-task benchmark and load test. |

| 2026-07-12 | Completed | Script quality was not release-gated: a full `ruff check scripts` reported 44 findings (42 intentional bootstrap-order E402 reports plus an unused import and a literal f-string), and browser coverage stopped at one unit test. This left executable research utilities and the default interactive path outside the stable verification boundary. | Added a root Ruff policy that documents the required `PROJECT_ROOT`/`sys.path` bootstrap exception while retaining all other script checks; removed the two non-exception findings; added script lint to the local quality loop and GitHub workflow. Added Playwright Chromium E2E that starts the real FastAPI and Vite services, advances one simulation step, verifies the deferred 3D view, and checks keyboard room selection plus pressed-state semantics. View-mode buttons now expose `aria-pressed`; Vitest explicitly collects only source unit tests so it does not misclassify browser specs. Generated browser/output artifacts are ignored to keep commits clean. | Evidence: `ruff check backend/app backend/tests scripts` passed; backend pytest 70/70; RAG Recall@3 3/3 = 1.0; `pip check` passed; Docker Compose config parsed; frontend Vitest 1/1, production bundle gate, and npm high-severity audit passed (default 2D initial JavaScript 183,062 bytes; deferred `three-runtime` 275.11 kB gzip); Playwright 2/2 passed against local FastAPI + Vite; `run_quality_loop.py` passed including `scripts_lint`; a live 2D screenshot is saved under ignored `output/playwright/20260712-hardening-2d.png`. Research boundary: these checks establish deterministic local software behavior and basic interactive accessibility semantics, not comprehensive WCAG conformance, real-provider LLM validity, load/concurrency performance, real-device safety, or real-home energy savings. Remaining risk: the deferred 3D bundle remains large, and production/research claims still require provider-versioned real-LLM trials and separately designed performance/accessibility studies. |

| 2026-07-11 | Completed | A custom reproduction suite could use a non-string or blank top-level `user_command`. `load_tasks()` accepted it, then `run_group()` coerced it through `str(...)`; direct pre-fix reproduction loaded `user_command: null` unchanged, while the runtime request became the measured prompt `"None"`. This silently changes the experimental condition instead of failing before execution. | `load_tasks()` now validates and normalizes the measured command before any runner starts: `user_command` must be a non-empty string, optional `current_room_id` must be null or a non-empty string, and the normalized fields must satisfy `AgentCommandRequest` (including known rooms). A focused regression covers null/blank commands, non-string rooms, and unknown rooms. | Evidence: direct pre-fix loader reproduction accepted `[{"task_id": "MALFORMED", "user_command": null, "current_room_id": false}]`, and a direct runtime request demonstrated `str(None)` becomes `user_command='None'`; focused reproduction-provenance tests 29/29, full backend pytest 70/70, backend/script Ruff, and `pip check` passed; frontend Vitest 1/1 and `npm run test:bundle` passed (default 2D initial JavaScript 183,014 bytes; deferred `three-runtime` 980.08 kB). Research boundary: this proves deterministic task-input contract enforcement only; it does not validate task-language semantics, manual labels, real-LLM behavior, concurrent services, browser flows, accessibility, or real-home outcomes. Remaining risk: a syntactically valid prompt can still be ambiguous or semantically unsuitable; high-assurance task suites still require human-reviewed semantics and controlled artifact storage. |

| 2026-07-11 | Completed | A custom reproduction suite could supply a non-list `pre_context_commands` value or non-object item. `run_pre_context_commands()` then treated the non-list as success or silently skipped the item, changing the context/history condition without an auditable failure. Direct pre-fix reproduction loaded both malformed suites and returned `True` for the non-list setup. | `load_tasks()` now validates optional context setup before execution: provided values must be lists, every item must contain only a non-empty `user_command` and optional valid `current_room_id`, and command/room whitespace is normalized. Invalid command shapes, unknown fields, and unsupported room IDs fail fast; a focused regression covers each case. | Evidence: focused reproduction-provenance suite 28/28, full backend pytest 69/69, backend Ruff, script Ruff with the repository's existing E402 exception, and `pip check` passed; frontend Vitest 1/1 and `npm run test:bundle` passed (default 2D initial JavaScript 183,014 bytes; deferred `three-runtime` 980.08 kB). Research boundary: this validates deterministic input-contract enforcement only; it does not validate the semantic meaning or real execution reliability of context commands, real-LLM behavior, concurrent services, or real-home outcomes. Remaining risk: a syntactically valid context command can still be semantically unsuitable for a task; high-assurance suites still require human-reviewed task semantics and controlled artifact storage. |

| 2026-07-11 | Completed | A reproduction task with failing `pre_context_commands` still recorded `success=true` whenever its final command succeeded. Since context commands establish the task state and/or dialogue history, the report's `Runtime success %` could overstate end-to-end experiment reliability and let a failed chain enter repeat-stability analysis. | The record-level runtime result now requires both pre-context and main-command success. `main_command_success` is retained for diagnosis, and a failed setup is surfaced as `pre-context command failed` rather than an unexplained execution error. Regression coverage constructs this exact counterexample and verifies the aggregate runtime success rate is 0%. | Evidence: focused provenance suite 27/27, full backend pytest 68/68, Ruff and `pip check` passed; frontend Vitest 1/1 and `npm run test:bundle` passed (183,014-byte initial JavaScript). Fresh mock artifact `output/qa-pre-context-runtime/20260711_152832` includes six context-chain tasks, records both success fields, and renders the revised definition. Research boundary: this validates local deterministic accounting only; it does not establish the correctness of context commands, real-LLM reliability, semantic accuracy, concurrent service behavior, or real-home performance. Remaining risk: malformed pre-context entries are currently ignored by `run_pre_context_commands()` and should be rejected during task-suite validation before making high-assurance claims. |

| 2026-07-11 | Quality gate passed (no substantiated regression) | Audited the current reproduction-runner integrity paths (task IDs, primary-repeat coverage selection, repeat-index validation, canonical fingerprints, and fail-closed stability statuses) after concurrent worktree changes. The exercised normal runner path produced no reproducible P0/P1 correctness, reproducibility, safety, or user-experience regression. | No product-code change: preserving the existing dirty worktree was required, and no independent defect met the loop's reproducibility threshold. | Evidence: full backend suite 67/67; backend Ruff and `pip check` passed; frontend Vitest 1/1 passed; `npm run test:bundle` passed with 183,014-byte initial JavaScript and deferred `three-runtime` 980.08 kB. Research boundary: these are deterministic local unit/build gates, not a real-Ollama run, browser end-to-end interaction, concurrent-service test, accessibility audit, or real-home validation. Remaining risk: later concurrent edits can invalidate this snapshot; rerun the full gate after reconciliation before making release or research-performance claims. |

| 2026-07-11 | Completed | Repeated-run stability accepted two failed semantic executions when each emitted the same canonical fingerprints for an empty semantic object and empty executable plan. The pre-fix reproduction returned `measured` with both stability percentages at 100%, which only describes a repeatable error path and could be misread as semantic/planning reproducibility. | `summarize_repeat_stability()` now fails closed with `failed_runs_no_stability_measurement` whenever any included repetition explicitly has `success=false`; it withholds both percentages and writes `failed_run_task_ids` to JSON and the Markdown report. A focused regression creates two failed, canonical-empty runs and verifies the status and withheld values. | Evidence: direct pre-fix reproducer produced two failed runs reported as 100%/100%; new focused regression passed, backend Ruff passed, backend pytest 67/67, frontend Vitest 1/1, and `npm run test:bundle` passed (2D initial JavaScript 183,014 bytes). The mock two-repeat smoke artifact at `output/qa-failed-run-stability/20260711_150426` contains two successful runs, zero failed task IDs, and `measured` 100% stability for one diagnostic task. Research boundary: the smoke run is mock, one task, and explicitly `smoke_test_insufficient_task_coverage`; it does not measure real-model reliability, benchmark accuracy, concurrent service behavior, controller outcomes, or real-home performance. Remaining risk: a response marked successful can still be semantically wrong (as the smoke artifact shows); stability is reproducibility evidence only, not correctness or safety evidence. |

| 2026-07-11 | Completed | `plan_action_fingerprint()` silently filtered non-dictionary entries and accepted a non-list payload as an empty action list. A malformed planner response could therefore hash identically to a valid no-op plan and be reported as repeated executable-plan stability. | The runner now preserves the raw `actions` value when building a record and returns an empty fingerprint for any non-list or mixed-type action payload. Existing repeat-stability validation consequently withholds the measurement as missing evidence; a focused regression covers string and mixed-action payloads and confirms they differ from a valid empty plan. | Evidence: direct reproduction previously produced the same SHA-256 for `[]` and a malformed string; after the fix malformed and mixed payloads produce an empty fingerprint. Focused provenance tests 25/25, full backend pytest 66/66, backend Ruff, frontend Vitest 1/1, and `npm run test:bundle` passed (initial JavaScript 183,014 bytes; deferred `three-runtime` 980.08 kB). Research boundary: this proves malformed-plan measurement fails closed and does not validate action-schema semantics, planner correctness, real-Ollama reliability, concurrent-service stability, or real-home outcomes. Remaining risk: JSON/CSV artifacts remain unsigned and mutable; high-assurance studies still need controlled artifact storage and integrity verification. |

| 2026-07-11 | Completed | Repeat-stability aggregation treated a task as a complete repeated unit whenever it had the requested number of rows, without checking that `repeat_index` covered each requested repetition exactly once. Duplicating the primary row twice could therefore be reported as measured 100% stability. | `summarize_repeat_stability()` now requires every complete task unit to contain integer indices `1..N` exactly once; duplicate, missing, non-integer, or boolean indices yield `invalid_repeat_indices` and withhold both stability percentages. Regression tests cover duplicate and missing indices. | Evidence: the focused provenance suite passes 25/25, the full backend suite 66/66, Ruff, frontend Vitest 1/1, and the 2D bundle guard all pass (initial JS 183,014 bytes). Research boundary: this proves record-level repetition integrity and does not establish model determinism, benchmark accuracy, concurrent-service stability, statistical independence, or real-home outcomes. Remaining risk: JSON/CSV artifacts are still unsigned and mutable; high-assurance studies need controlled storage and artifact-integrity verification. |

| 2026-07-11 | Completed | Repeat-stability aggregation accepted any non-empty fingerprint value. Consequently, an instrumentation fallback such as `not_recorded` repeated twice was deduplicated and reported as `measured` with 100% semantic and executable-plan stability, even though no generated fingerprint evidence existed. | Stability aggregation now requires canonical lower-case SHA-256 digests for semantic-runtime, semantic-output, and plan-action fingerprints. It fails closed with `invalid_fingerprint_format`, withholds both percentages, and reports the task IDs invalid for each field in JSON and Markdown. | Evidence: focused provenance tests 26/26 and the complete backend suite 67/67 passed; backend Ruff, pip check, frontend Vitest 1/1, and the 2D bundle guard (183,014-byte initial JS) passed. The two-repeat mock smoke artifact at `output/qa-invalid-fingerprint-format/20260711_150603` has no invalid fingerprints and reports `measured` with 100% stability for one diagnostic task; it is explicitly `smoke_test_insufficient_task_coverage`. Research boundary: digest-shape validation confirms generated-format evidence only; it does not establish hash preimage semantics, artifact integrity, real-Ollama reliability, benchmark accuracy, concurrent-service stability, or real-home outcomes. Remaining risk: unsigned JSON/CSV artifacts can still be altered while retaining syntactically valid hashes; use controlled, immutable storage for high-assurance audit trails. |

| 2026-07-11 | Completed | Reproduction task loading accepted an empty or duplicate `task_id`, although that value is the join key for manual semantic annotations, primary performance records, and repeat-stability units. A malformed custom suite could therefore fail only after execution or be mis-grouped by downstream analysis. | `load_tasks()` now rejects empty task lists, missing/non-string IDs, and duplicate IDs before any experiment runner starts; surrounding whitespace is normalized once and persisted as the canonical key. Regression coverage exercises all three invalid-suite cases. | Evidence: focused provenance tests 22/22, full backend pytest 63/63, backend Ruff, frontend Vitest 1/1, and the production 2D bundle guard passed (initial JS 183,014 bytes). Research boundary: this proves input-key integrity and fail-fast behavior only; it does not validate task-language labels, semantic accuracy, controller outcomes, real-Ollama reliability, or real-home behavior. Remaining risk: task IDs are now unique within one loaded file, but cross-file/version identity still relies on the recorded task-suite fingerprint and is not cryptographically signed or immutable. |

| 2026-07-11 | Completed (quality gate partially blocked by unrelated worktree changes) | `--repeat-count` is documented as a within-task stability measurement that adds no independent task coverage, but the overall summary, category summary, and charts consumed every repeat. A one-task, two-repeat mock reproduction wrote `sample_count=2` for both overall and category metrics although stability correctly reported one task unit. | Added `primary_performance_records()` and made coverage-based overall metrics, category metrics, and chart data consume only preregistered isolated repeat 1. The full CSV and repeated-run stability calculation retain every repeat; summary/report metadata now state the split and the report labels primary-repeat metrics. A regression test verifies that two records for one task produce one performance sample and one chart row. Evidence: focused regression 1/1 passed; backend Ruff passed; fresh mock artifact `output/qa-repeat-coverage-fixed-current/20260711_145800/summary.json` has one executed task, one performance record, two stability records, one overall/category sample, one power-chart row, and measured stability for one task unit. Frontend Vitest 1/1 and `npm run test:bundle` passed (initial JavaScript 183,014 bytes). The current full backend suite is blocked by four non-loop failures in `test_reproduction_provenance.py`: one task-ID error-message expectation and three tests whose synthetic records omit the current worktree repeat-index field. Research boundary: repeat 1 is a deterministic primary-analysis convention, not an estimate of across-run performance or real-model reliability; later repeats remain only a within-task consistency check. Remaining risk: real runs can vary after the primary repetition, and the unaddressed external repeat-index test-contract changes must be reconciled before a clean full-suite gate is claimed. |

| 2026-07-11 | Completed | A one-task mock reproduction diagnostic returned `success=true` and `completed=true` while its predicted intent was wrong (`lighting_comfort_control` rather than the P001 reference `basic_light_control`). The report's leading `Success %` column could therefore be read as semantic task success despite a 0% intent score. | Renamed the aggregate and category column to `Runtime success %` and documented that it only means the runner finished without runtime/execution error; semantic correctness and task completion remain separate metrics. A regression test renders this exact counterexample and requires the explicit distinction. | Evidence: P001 two-repeat mock diagnostic at `output/qa-runtime-success-label/20260711_145747` records runtime success and completion at 100% with intent accuracy 0%, and renders the revised header/note; the focused new regression passed. Backend application/tests Ruff, full backend pytest (63/63 at the time of that run), frontend Vitest (1/1), and the 2D bundle guard passed. A later focused provenance rerun encountered two unrelated existing stability-fixture assertions after concurrent working-tree changes, so it is not counted as green evidence for this repair. Research boundary: clearer labeling prevents conflating transport/execution health with semantic correctness, but it does not validate curated labels, real-LLM accuracy, controller outcome quality, or real-home effectiveness. Remaining risk: readers can still selectively quote any individual metric, so reports must be interpreted as the complete metric set and their stated applicability. |

| 2026-07-11 | Completed | Repeat-stability aggregation checked that semantic runs used one non-empty runtime fingerprint, but it treated two missing semantic-output or executable-plan fingerprints as one identical value and could report 100% stability after an instrumentation/schema regression. | `summarize_repeat_stability()` now fails closed with `missing_semantic_or_plan_fingerprint`, withholds both stability percentages, and emits the affected task IDs separately for semantic and plan fingerprints. The rendered report exposes those missing-ID columns and documents non-empty output hashes as a measurement precondition. | Evidence: focused provenance tests 18/18 (including an empty-hash reproduction), full backend suite 59/59, Ruff, frontend Vitest 1/1, and the 2D bundle guard all passed. The two-repeat mock smoke artifact at `output/qa-missing-output-fingerprint/20260711_120559` has no missing fingerprints and reports `measured` with 100% stability for its one diagnostic task. Research boundary: this rejects absent measurement evidence but does not validate hash-field semantic completeness, real-Ollama reliability, benchmark accuracy, concurrent-service stability, or real-home outcomes. Remaining risk: JSON/CSV artifacts are not signed or immutable; controlled storage and artifact-integrity controls are still needed for high-assurance audit trails. |

| 2026-07-11 | Completed | The in-process API limiter kept one deque for every distinct client key with no global capacity or idle-key eviction. A client-key flood could therefore grow process memory without bound, while an expired key was only cleaned if that same key returned. | Added a configurable `API_RATE_LIMIT_MAX_CLIENT_KEYS` bound (default 10,000). Before adding a new key, the limiter reclaims expired buckets; if active capacity is still full, it fails closed with 429 rather than allocating more memory. Regression test exercises two admitted keys, rejects a third at capacity, then confirms a later request succeeds after the window expires. Evidence: focused API-hardening tests 5/5, full backend suite 57/57, Ruff, frontend Vitest 1/1, and production bundle guard all pass. Research implication: public API load controls now have an explicit memory bound, reducing an availability confound for interactive simulator studies. Remaining risk: this is process-local, IP-keyed limiting and can deny new clients during a sustained key flood; production-scale deployment still needs a trusted proxy identity policy and shared gateway/rate-limit store. |
| 2026-07-11 | Completed | Repeated-run stability was calculated from semantic and plan-output hashes without confirming that all repetitions used the same semantic runtime fingerprint. A model, decoding configuration, endpoint, or semantic-adapter change between repeats could therefore still be reported as stable. | `summarize_repeat_stability()` now records the number of observed semantic runtime fingerprints and withholds both stability percentages with `inconsistent_semantic_runtime_fingerprint` unless all semantic records have one non-empty matching fingerprint. The report renders that count and documents the precondition; a regression test proves identical outputs under two fingerprints are rejected. Evidence: the focused provenance suite 17/17, full backend suite 58/58, Ruff, frontend Vitest 1/1, and bundle guard passed. The local mock smoke artifact at `output/qa-runtime-fingerprint-stability/20260711_114559` has two repetitions for each of two semantic groups, exactly one runtime fingerprint per group, and 100% within-task stability. Research boundary: this proves the measurement guard and deterministic mock path only; it is not real-Ollama reliability, benchmark accuracy, concurrent-service stability, or real-home evidence. Remaining risk: an unversioned real runtime can still be internally consistent for a run but is not version-specific model evidence. |
| 2026-07-11 | Completed | Default 2D startup eagerly imported the manually named `three-runtime` chunk (1,127.88 kB before gzip), despite `HouseScene` being rendered through `React.lazy`. The manual Rollup chunk absorbed transitive `@react-three` dependencies, including Vite's dynamic-import helper, so the default 2D user path preloaded the 3D runtime. | Preserved the 3D manual chunk but isolated React and Vite's preload helper into entry-safe chunks; added `npm run test:bundle`, which rebuilds the frontend and fails if the entry statically imports or HTML preloads a 3D chunk, or if all initial JavaScript reaches the 500 KiB budget. Evidence: initial JavaScript is now 183,014 bytes (entry + two preloads); the 980.08 kB `three-runtime` remains deferred; frontend Vitest 1/1, bundle regression, backend pytest 56/56, and Ruff all pass. Research implication: the default simulator UI now preserves the stated lazy 3D boundary, reducing an avoidable initial-load confound for interactive studies. The remaining deferred 3D chunk is still large; this build-artifact check is not a real-network, device-performance, accessibility, semantic-quality, controller-outcome, or real-home validation. |

> 2026-07-10 — Non-semantic baseline target-metric applicability (completed): the fixed-policy baseline constructs an oracle semantic object only to reuse the feedback helper; a one-task diagnostic therefore reported 100% target exact match and IoU despite `semantic_applicable=false`. Target-range metrics are now inapplicable for groups without natural-language parsing, so group and category summaries render `N/A`; a regression test prevents the injected feedback reference from being reported as parser accuracy. Research implication: semantic-parameter comparisons are limited to methods that actually interpret input. This does not validate labels, controller outcomes, or real-home behavior.

| Date | Status | Topic | Evidence and research implication |
|---|---|---|---|
| 2026-07-11 | Completed | Reproduction stability fingerprints were referenced by `build_record()` but missing from the active runner during the first QA pass, preventing planning-only and fixed-policy baseline runs from emitting auditable records. The restored helpers hash only semantic interpretation fields and the ordered executable action payload; non-semantic baselines record the explicit sentinel `not_applicable`, preventing an oracle feedback object from being mistaken for parser output. | Evidence: focused provenance tests 16/16, full backend suite 56/56, frontend test 1/1, and production build passed. The two-repeat mock planning smoke artifact at `output/qa-reproduction-stability-fingerprint/20260711_104220` records identical semantic and plan fingerprints for P001 and reports both stability measures as 100%; the paired one-task artifact remains `smoke_test_insufficient_task_coverage`. Research implication: repeated-run stability is mechanically traceable for selected semantic and executable-plan fields, while non-semantic baseline applicability is explicit. This deterministic mock one-task integration check is not real-Ollama reliability, benchmark accuracy, statistical evidence, or real-home performance. |
| 2026-07-11 | Completed | Non-semantic repeat-fingerprint boundary | Evidence: experiment records now persist a semantic-output fingerprint for repeated-run stability, but a non-semantic fixed-policy baseline emitted an empty value. In CSV analysis, that value is indistinguishable from a missing or failed fingerprint and could blur the boundary between a parser measurement and a non-applicable metric. Fix: persist the explicit `not_applicable` sentinel for groups that do not interpret natural language, while retaining canonical SHA-256 fingerprints only for semantic groups; a regression assertion covers the baseline record. Validation: all 56 backend tests pass; `output/qa-loop-20260711/20260711_104121` generated two isolated repeats for both a fixed-policy and a mock-semantic planning group, with the baseline marked `not_applicable` and the semantic group reporting 100% within-task stability. Research implication: this makes inapplicability machine-auditable; the one-task mock smoke result is explicitly not a benchmark-wide, real-Ollama, controller-outcome, or real-home claim. |
| 2026-07-10 | Completed | Semantic-label provenance boundary | Evidence: the versioned 36-task reference had complete control-goal and target-range coverage plus a suite hash, but it retained no machine-readable status for independent review, inter-annotator agreement, or ambiguity adjudication. Coverage could therefore be mistaken for validated ground truth. Fix: introduced `semantic_reference_v4` and required `semantic_annotation_provenance`; generated reports now expose the protocol, simulated-reference type, explicit `not_recorded` evidence statuses, and `incomplete_annotation_provenance` label-quality status. Validation: focused provenance tests reject a versioned suite missing the provenance object and confirm the reportable boundary. Research implication: the benchmark now makes its label-validity limitation auditable; it does not establish label correctness, agreement, real-Ollama quality, controller outcomes, or real-home behavior. |
| 2026-07-10 | Completed | Semantic target-range ground truth | Evidence: the fixed 36-task suite recorded intent, room, scope, control direction and device categories, but it did not retain an independent expected semantic target range or score parser-supplied temperature, illuminance and humidity ranges. A parser could therefore select the right device and direction while emitting a materially wrong control parameter without changing any semantic metric. Fix: introduced `semantic_reference_v3` with versioned target-range annotations for all 36 task IDs; the runner validates coverage, records expected/predicted target objects and key counts, reports strict target exact match plus mean interval-IoU overall/by category, and classifies a target-only mismatch as `semantic_target_error`. Target metrics remain `N/A` for the non-semantic fixed-policy baseline. Validation: `output/qa-target-ground-truth/20260710_143842` records 36/36 labels and exposes 61.11% strict target matching with 83.13% target-range IoU for the reproducible mock diagnostic; the full backend suite (52 tests) passes. Research implication: parameter-level semantic evidence is now traceable and can distinguish partial overlap from exact execution intent, but the curated targets and mock run remain simulated reference/baseline evidence, not real-Ollama, controller-outcome, or real-home validation. |
| 2026-07-10 | Completed | Control-goal semantic ground truth | Evidence: the versioned 36-task reproduction suite measured intent, room, scope, and planned device categories but had no independently stored `control_goal` reference. A parser could reverse an `on`/`off` direction or emit `set_target` and still receive a high semantic score, despite the safety-relevant execution difference. Fix: added the manually curated `semantic_reference_v2` goal annotations (`turn_on`/`turn_off`/`set_target`) for all task IDs; the runner validates coverage, writes expected/predicted/correct goal values per record, reports overall/category directional accuracy and explicit directional failures, and emits suite SHA-256 plus annotation-schema/coverage provenance. Validation: focused tests verify complete labels and a directional mismatch; the mock smoke artifact includes the new CSV/report fields. Research implication: fixed-suite semantic claims now expose action direction, but the manual labels remain simulated references and do not establish real-home safety or generalization. |
| 2026-07-10 | Completed | Smoke-run performance-claim guardrail | Evidence: the latest versioned-real-Ollama reproduction artifact executed only P001 (one task and one category) yet its “Overall Metrics” table displayed 100% semantic accuracy. That result is a single-case integration observation, not a benchmark-wide estimate. Fix: the runner now persists source/executed task and category coverage, a machine-readable claim status, and a conservative minimum-coverage decision; reports visibly warn when a limited or undersized suite is diagnostic only. Research implication: limited runs remain useful for runtime/provenance checks but cannot be cited as local-model benchmark performance; even a complete fixed suite remains descriptive simulated evidence rather than a population or real-home claim. |
| 2026-07-10 | Completed | Real-Ollama decoding reproducibility | Evidence: the live local-Ollama client fixed `temperature=0.0` but sent no generation seed, and real-mode experiment/runtime provenance could not distinguish sampling streams. The configured `qwen3:8b` endpoint supports OpenAI-compatible `seed`; a no-seed real run is therefore not fully specified even with an immutable model digest. Fix: added explicit `REAL_LLM_TEMPERATURE` and `REAL_LLM_SEED` controls (deployment defaults: `0.0` and `42`), transmits both with every real completion, records them in semantic and life-simulation event traces, and includes them in real-mode runtime fingerprints for both experiment runners. Validation: an isolated client test captures the request payload and verifies both controls; 25 relevant backend tests pass. Research implication: real semantic runs can now identify the requested decoding stream as well as the model revision and adapter code. This is reproducibility metadata, not a guarantee of bitwise equality across Ollama versions, hardware, concurrency, or model-server implementations. |
| 2026-07-10 | Completed | Reproduction benchmark real-model provenance | Evidence: the reproduction benchmark's real-semantic groups always loaded `backend/.env` and persisted neither an immutable model revision nor a non-secret runtime/adapter fingerprint. The running dashboard uses `deploy/backend.env`; an initial host smoke run against that file failed because Docker's `host.docker.internal` endpoint is refused outside the container, so the benchmark could not both target the UI configuration and execute. Fix: added `--env-file` with shell-variable precedence, a constrained `host.docker.internal` to localhost bridge for this host-side runner, per-record model/revision/source/status/adapter/runtime fingerprint fields, and top-level configured/effective endpoint provenance plus a report boundary. Local Ollama revisions come from an explicit setting or bounded `/api/tags` lookup. Research implication: a result supports a version-specific local-Ollama semantic claim only with `effective_llm_mode=real` and `provenance_status=versioned_real_model`; mock/unversioned outputs remain reproducibility artifacts, not model-version performance or real-home evidence. |
| 2026-07-10 | Completed | Research replay previously verified only execution state transitions | Evidence: `POST /api/research/run` accepted a structured semantic input, yet its JSONL log retained only the resulting plan/actions; `ReplayEngine` reapplied recorded tool calls and compared state hashes. A successful replay therefore could not demonstrate that the semantic input was traceable or that the deterministic planner would produce the recorded plan. Fix: each agent step now records `agent_semantic_input` with a canonical SHA-256 fingerprint and stores planner/input fingerprints; replay validates input integrity, recomputes each recorded planner output, returns `checked_plans`, and labels legacy logs through `unverified_plans`. Validation: `backend/tests/test_research_api.py` confirms one plan and one tick are both verified. Research implication: new runtime logs support a bounded semantic-to-plan-to-execution reproducibility claim for the deterministic structured planner; they still do not reproduce external LLM sampling, validate semantic correctness, or establish real-home behavior. |
| 2026-07-10 | Completed | Docker-to-experiment real-model configuration alignment | Evidence: the live Docker API was configured for real `qwen3:8b` (health check confirmed it), but its settings live in `deploy/backend.env`; the host-side personalization runner only relied on the default `backend/.env` path, so a `--semantic-mode real` ablation could silently miss the deployment configuration or fall back to unrelated defaults. Fix: added an explicit `--env-file` input (defaulting to `backend/.env`), loaded before any LLM client/provenance is created, and documented `--env-file deploy/backend.env` for Docker-backed runs. Shell variables still take precedence, and a real run now fails early when neither a selected file nor credentials are available. Research implication: a real semantic artifact can be tied to the explicitly selected runtime configuration and its existing model/runtime fingerprints; this improves execution reproducibility but does not itself validate model quality, real-home behavior, or replace the required immutable model revision. |
| 2026-07-10 | Completed | Versioned real-model experiment provenance | Evidence: personalization records kept only a mutable real-model alias (for example `qwen3:8b`), while the semantic adapter source, endpoint/decode configuration, and immutable model revision were absent. A local alias can be retagged, so a result could not be reproduced or safely attributed to a specific local-Ollama version. Fix: records and paired reports now retain a non-secret semantic runtime fingerprint, semantic-adapter SHA-256, model revision, and provenance status; real runs without `REAL_LLM_MODEL_REVISION` are labeled `unversioned_real_model`, and strict pairing rejects runtime-fingerprint mismatches. Research implication: a real semantic result supports a version-specific local-model claim only when the immutable revision is recorded; mock artifacts remain reproducible simulator baselines, not real-model or real-home evidence. |
| 2026-07-10 | Completed | Generated safety-artifact freshness | Evidence: after the source suite added P006/SFI-002, the persisted personalization CSV/protocol/summary still described only P001-P005, had a stale runner SHA-256, and reported three rather than six paired safety-fixture runs per method/condition. Fix: regenerated the deterministic mock factorial artifacts from the current runner; they now contain 108 rows, P001-P006, matching runner/task-suite fingerprints, and six paired safety-fixture streams (two task clusters x three seeds) for each method/condition. Research implication: the saved CSV, protocol, and paired report now identify the exact two-fixture code/protocol they represent; this refresh is provenance repair, not new real-Ollama or real-home performance evidence. |
| 2026-07-10 | Completed | Safety-fixture diversity for review intervention | Evidence: the prior ablation had only SFI-001 (unsafe window opening), so its safety-review evidence covered one actuator/hazard class and every safety-fault comparison had a singleton task cluster. Fix: added SFI-002, a deterministic high-speed fan fault under the existing strong-airflow health constraint; `SafetyAgent` now detects and blocks this distinct actuator violation, and the protocol records an explicit two-fixture catalog plus a strict non-generalization boundary. The bootstrap guard now requires three task clusters, so the two-fixture safety scenario retains descriptive point effects but no interval. Research implication: the suite now demonstrates two named simulated actuator-fault interceptions (window opening and fan speed), but it remains a small, deterministic, non-calibrated fixture set rather than evidence of real-home safety, planner-error prevalence, complete safety coverage, or uncertainty-supported safety performance. |
| 2026-07-10 | Completed | Singleton safety-fixture uncertainty guardrail | Evidence: the P005 deterministic safety-fault scenario has one task cluster (with repeated root-seed streams), but the paired Markdown report displayed a degenerate 95% bootstrap interval as though it quantified uncertainty. Fix: records now emit a bootstrap interval only when at least two distinct task clusters exist; singleton scenarios retain descriptive point effects but store `paired_bootstrap_95_ci=null`, `uncertainty_status=insufficient_task_clusters`, and render `N/A (insufficient task clusters)`. Research implication: P005 remains evidence only of named-fixture interception, without a false precision signal or a population uncertainty claim; diverse independent intervention tasks are still required for interval-based safety comparison. |
| 2026-07-10 | Completed | Multi-objective safety construct traceability | Evidence: the personalization ablation used safety inside weighted utility, but did not persist a safety score or metric definition in its CSV/paired report; its prior boolean also assigned the same 100 score to a final plan with a medium issue and a plan with no issue. Fix: score remaining issues on the final executable plan with the explicit deterministic `final_executable_plan_severity_penalty_v1` construct (none=100, one medium=60, any high=20; floor 20); records now retain the score, measurement ID, and remaining high/medium counts, pairing requires the measurement ID, and the paired summary reports `safety_score`. A regenerated 90-row mock factorial run shows P005 baseline rows at 20 with one high issue and reviewed rows at 100 with none remaining. Research implication: the safety contribution to utility is reconstructable and the SFI-001 intervention result is separable from other objectives; this ordinal simulated severity rule is not a calibrated risk probability, real-home safety estimate, or population claim. |
| 2026-07-10 | Completed | Experiment artifact implementation provenance | Evidence: the current personalization runner includes the deterministic P005 safety-fault task, but the existing generated protocol/report still listed only P001-P004. Because no source or task-suite fingerprint accompanied the CSV, protocol, or summary, a stale artifact could be mistaken for current evidence. Fix: records now persist SHA-256 fingerprints for the exact runner source and ordered task fixtures; protocol and Markdown summary expose the same values; pairing rejects mixed implementation fingerprints. The refreshed mock run regenerates all artifacts with P001-P005. Research implication: an experiment result is traceable to the code and fixture suite that produced it, and any fingerprint change requires regeneration; this establishes provenance, not real-home validity or real-Ollama performance. |
| 2026-07-10 | Completed | Safety-review intervention validity | Evidence: the prior personalization suite recorded zero safety-action changes across all 72 mock rows because health constraints were already resolved inside planning, leaving the downstream safety reviewer with no unsafe candidate to intercept. Its `passed` flag also represented the existence of a pre-review issue rather than the safety of the final executable plan, which could have penalized a successful intervention. Fix: added the named deterministic `SFI-001` unsafe-window planner-fault fixture (P005), injected only after normal planning and only in the experiment runner; records retain scenario/fault/detection/blocking provenance, summaries stratify ordinary control from the fixture, and baselines receive risk assessment without reviewer intervention. A regenerated 90-row mock factorial run has 18 P005 rows: both multi-agent methods block all 6 injected cases per method, while the single-agent baseline blocks 0; the 4 ordinary task clusters remain reported separately. Research implication: this supports only deterministic interception of the named simulated fault, not ordinary-task safety, planner-error prevalence, real-home safety, or population inference. |
| 2026-07-10 | Completed | Task-comfort outcome construct alignment | Evidence: the personalization ablation's `comfort_score` came from `average_overall_score` across the entire virtual home, while the utility and satisfaction evaluator scored only the occupied command room. A direct single-room probe found 98.88 whole-home comfort versus 89.89 occupied-room comfort after the same 30-minute task horizon, so the reported comfort outcome and multi-objective score were different constructs. Fix: the evaluator now records `comfort_measurement`; ablation task comfort uses the same occupied-room score and pairs that measurement definition across controllers, with whole-home averaging retained only as an explicit no-occupant dashboard fallback. Research implication: task-level comfort effects are now traceable to the resident exposed to the command and aligned with utility/satisfaction; they remain simulated subjective-comfort proxies, not real resident reports. |
| 2026-07-10 | Completed | Multi-objective energy construct alignment | Evidence: the personalization ablation reported a fixed 30-minute `energy_kwh` outcome, but its utility evaluator previously derived the energy component from the final state's instantaneous baseline-power saving rate and multiplied it until most savings saturated at 100. The score therefore could not be reconstructed from the reported outcome measurement. Fix: the factorial runner now scores energy from the same post-control cumulative-energy increment against a common initial-state baseline-power projection for the fixed horizon, records the actual/reference kWh, relative saving rate, measurement type, and bounded energy score, and summarizes that score separately. A fresh 72-record mock run used `post_control_horizon_kwh` for every row, one 0.01340 kWh matched baseline, and energy scores spanning 0.00-83.21 rather than an early ceiling. Research implication: the utility energy component is now traceable to the stated simulator horizon and is comparable within paired conditions; the reference remains a simplified initial-state projection, not a calibrated physical-building or real-home energy baseline. |
| 2026-07-10 | Completed | Personalization-ablation uncertainty unit | Evidence: the 72-row factorial output reported 12 paired units per condition although each condition contains only four task IDs and three root-seed streams per task; nominal streams were exact duplicates for a task. Treating all streams as independent bootstrap observations causes pseudoreplication and can make intervals too narrow. Fix: strict record-level matching still requires condition, seed, disturbance stream, initial state, horizon, and semantic backend, but effects are now averaged over paired seed streams within each task and bootstrapped across task clusters. Reports expose both task-cluster and paired-run counts. Research implication: uncertainty now represents variation across the fixed task suite rather than an inflated number of repeated streams; it remains descriptive and is not a population-level significance claim. |
| 2026-07-10 | Completed | Multi-agent ablation mechanism coverage | Evidence: paired mock checks found `single_agent` and `multi_agent_static` produced identical task outcomes for all four nominal tasks (completion, comfort, energy, utility, and satisfaction), while the prior report did not state whether safety review or feedback correction ever changed a trajectory. Fix: safety review now emits `actions_changed`; each ablation record retains safety issue/action-change and feedback-correction traces; paired JSON/Markdown reports separate review coverage from feedback coverage. Because feedback correction is common to every controller, only a safety action change establishes that the multi-agent review branch intervened; zero review changes are explicitly marked `not_observed_non_discriminative`. Research implication: zero-effect rows cannot be overread as evidence against the multi-agent review loop, nor can they support its claimed benefit; future task suites need traceable safety-review intervention cases before making review-effect claims. |
| 2026-07-10 | Completed | Personalized-ablation energy validity | Evidence: the 72-row factorial CSV reported `energy_kwh = 0.0` for every controller, condition, seed, and task because a command was scored at the zero-duration instant after actions. Fix: each fresh deterministic task now advances a fixed 30-minute post-control, non-realtime observation window; the records expose the horizon and evaluation time step, and pairing requires them to match. Comfort, cumulative energy, utility, and satisfaction are read from that common outcome state, while completion remains the immediate feedback outcome. Validation: a fresh 72-row mock run produced positive energy values in every group (0.00450–0.40237 kWh) and regenerated protocol/paired-bootstrap artifacts. Research implication: energy effects are now duration-defined simulator measurements, not zero-time snapshots; they remain limited to the fixed 30-minute simulated horizon and must not be generalized to real homes. |
| 2026-07-10 | Completed | Personalization semantic provenance | The ablation defaulted to mock semantics but its records and paired reports did not identify the configured/effective semantic backend or model, so mock results could be mistaken for local-Ollama evidence. Records now capture configured/effective semantic modes and a non-secret model identifier; reports pair only identical semantic backends; `--semantic-mode mock|real` makes the choice explicit and keeps mock the reproducible default. Results only support local-Ollama semantic claims when the effective mode is `real` and the recorded model identifier names the evaluated service. |

| 状态 | 优先级 | 项目 | 验收标准 |
|---|---|---|---|
| Completed | P0 | Personalization ablation fairness | Replaced the confounded `personalized_robust` pseudo-method with a 3-method x 2-condition paired-seed protocol. CSV records now expose condition and every disturbance parameter; `ablation_protocol.json` restricts claims to within-condition, within-seed comparisons. `backend/tests/test_personalization_experiment_protocol.py` verifies the contract, so robustness effects are attributable to the controller rather than an uncontrolled change in test difficulty. |
| 完成 | P0 | 会话隔离与并发串行化 | 两个会话的设备状态互不影响；同会话操作受锁保护。 |
| 完成 | P0 | API 鉴权、限流、请求追踪 | 设置 `API_AUTH_TOKEN` 后匿名 API 请求返回 401；响应包含请求 ID。 |
| 完成 | P1 | 前端请求韧性与高频控制 | 有超时、会话头、认证 Cookie 和滑块提交控制。 |
| 完成 | P1 | 测试、CI、依赖审计 | CI 执行 Ruff、pytest、Vitest、构建、依赖一致性及高危 npm 审计。 |
| 完成 | P2 | RAG 评测与可替换检索 | 有版本化评测集、Recall@3 阈值和稳定的 `query()` 接口。 |
| 完成 | P1 | 3D 户型视觉适配 | 修正初始相机与可缩放范围；宽屏视觉复核确认完整户型进入画面。 |

## 已验证的研究严谨性改进

| 日期 | 状态 | 问题与证据 | 最小修复 | 研究含义与验收证据 |
|---|---|---|---|---|
| 2026-07-10 | Completed | Local-Ollama real-mode provenance was unnecessarily unversioned: the running `qwen3:8b` service exposes an immutable `/api/tags` digest, but the personalization runner required a manually configured `REAL_LLM_MODEL_REVISION` and otherwise recorded only a mutable model alias. The Docker deployment also uses `host.docker.internal`, which a host-launched runner cannot query directly. | Real semantic provenance now prefers an explicit revision and otherwise performs a bounded, failure-safe `/api/tags` lookup for an OpenAI-compatible local Ollama endpoint. For the Docker-only hostname it may query localhost for the same configured model alias and records the distinct fallback source. Records and reports retain the immutable digest and its source; unreachable/non-Ollama endpoints remain explicitly `unversioned_real_model`. | Real semantic artifacts can now identify the exact locally served Ollama model when that digest is available, rather than relying on a mutable alias. This improves reproducibility of local-model experiments only; it does not establish semantic accuracy, controller effectiveness, deterministic decoding behavior, or real-home validity. |
| 2026-07-10 | 完成 | `scripts/run_reproduction_experiments.py --real-mode mock` 会将真实语义组实际降级为 mock，但汇总与 Markdown 报告仍展示 “Real …” 组名；这会使本地烟雾测试被误读为真实或本地 Ollama 的语义性能。 | 记录配置语义模式与实际语义模式；当降级发生时，在 CSV、图表、汇总和报告中统一标注 `semantic=mock`，并在报告写明该结果不能作为真实/本地 Ollama 性能证据。 | `backend/tests/test_reproduction_provenance.py` 覆盖 mock 覆盖与真实执行两种标签；单任务 mock 复现实验成功生成带有效模式列的报告。该修复使基线/消融结果的模型来源可追溯，避免不受支持的性能结论。 |
| 2026-07-10 | 完成 | `real_semantic_planning` 消融组设计为只做语义与规划、不执行设备动作，但记录和汇总仍从未变化的初始状态计算完成率、节能率与舒适度，可能把不可比的状态快照误读为方法效果。 | 为执行结果增加 `outcome_metrics_applicable`；对不执行动作的组将完成、能耗和舒适度字段记为不适用，汇总使用 `null`、Markdown 显示 `N/A`，并从对应图表数据中排除。 | `backend/tests/test_reproduction_provenance.py` 覆盖单任务 planning-only mock 运行，验证原始结果为空、汇总为 `None` 且完成率/节能率图表不包含该组；完整 smoke 报告可区分语义能力与闭环控制结果，使消融比较只在可比指标上成立。 |
| 2026-07-10 | 完成 | 个性化消融 CSV 仅对 `personalized` 方法写入效用和满意度；`single_agent` 与 `multi_agent_static` 的同列为空，导致多目标结果无法进行方法间比较。 | 将规划时的偏好注入与结局评估解耦：所有方法均使用同一固定偏好剖面和权重评分，只有个性化控制器在规划阶段获得该剖面；指标名称改为 `profile_weighted_multi_objective_v1`，避免把评估器误称为控制器。 | `test_static_controller_can_use_a_fixed_profile_for_outcome_evaluation` 与消融脚本契约测试确保三种方法均生成数值效用/满意度，并将统一评估规则写入 `ablation_protocol.json`。因此，方法差异可以归因于控制逻辑而非指标缺失或不一致的偏好权重。 |
| 2026-07-10 | 完成 | 个性化消融在每个 method/condition/seed 内复用同一个 `SmartHomeEnvironment` 和 `TaskRunner`。前一任务的设备状态、时间步和上下文记忆会进入后一任务；而鲁棒性噪声依赖时间步，控制器动作数不同会令宣称“同 seed 配对”的扰动流实际不一致。 | 为每个 task 创建新的确定性环境和 `TaskRunner`，以隔离设备状态与上下文；用 `SHA-256(experiment_seed, task_id)` 派生并记录 `disturbance_seed`，同时写入 `task_id`、`initial_time_step=0` 和 `initial_state_fingerprint`。协议明确比较必须在 condition、实验 seed 和 task_id 均一致时进行。 | `backend/tests/test_personalization_experiment_protocol.py` 验证 task seed 稳定且任务间不同，三种控制器对同 task 使用同一扰动 seed，并验证每条记录从 time step 0 与相同初态指纹开始。这样任务级指标可归因于控制器，而不受前序任务轨迹或隐藏随机流影响；该仿真结论仍限于记录的固定任务集与扰动模型。 |
| 2026-07-10 | 完成 | 个性化消融仅输出原始 CSV；即使实验设计已按 condition/seed/task 配对，仍没有按配对单位聚合或报告不确定性，容易把少量固定任务上的均值差异误读为稳定方法优势。 | 生成 `ablation_summary.json` 和 `ablation_summary.md`：对每个候选控制器与 `single_agent`，严格检查 condition、seed、task、扰动流与初态指纹完全配对后，报告候选减基线的均值效应及固定随机种子的 2,000 次配对非参数 bootstrap 95% 区间；协议和报告明确这不是显著性检验。 | `test_paired_summary_reports_reproducible_effects_and_intervals` 验证汇总与区间逐次一致，`test_paired_summary_rejects_initial_state_mismatch` 拒绝失配记录；完整实验同时保存机器可读与 Markdown 报告。结果只能描述这组已记录仿真任务内的不确定性，不能推断真实住户、真实设备或总体显著性。 |
